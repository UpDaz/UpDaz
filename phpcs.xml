<?xml version="1.0"?>
<ruleset name="PHPCS-PSR2">
    <description>The coding standard for php files into Laravel Project</description>
    <rule ref="PSR1"/>
    <rule ref="PSR12">
        <exclude name="Generic.Files.LineLength"/>
    </rule>

    <file>app</file>
    <file>bootstrap</file>
    <file>config</file>
    <file>database</file>
    <file>routes</file>
    <file>resources/css</file>
    <file>resources/js</file>

    <exclude-pattern>bootstrap/cache/*</exclude-pattern>
    <exclude-pattern>bootstrap/autoload.php</exclude-pattern>
    <exclude-pattern>*/migrations/*</exclude-pattern>
    <exclude-pattern>*/seeds/*</exclude-pattern>
    <exclude-pattern>*.blade.php</exclude-pattern>
    <!-- Laravel's stock front controller: defining LARAVEL_START then
         bootstrapping the app is an inherent side effect of this file's
         role, not something PSR1's "one symbol or side effects" rule
         can be satisfied for without deviating from the framework. -->
    <exclude-pattern>public/index.php</exclude-pattern>

    <!-- Show progression -->
    <arg value="p"/>
</ruleset>

